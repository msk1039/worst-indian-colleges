/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/college-votes-v1",
    // output: "export", // <=== enables static exports
  reactStrictMode: true,
};

module.exports = nextConfig;
